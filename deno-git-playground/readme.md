# Notes

Here I have some small scripts I wrote to experiment with Deno and git.

They are quicker to iterate than using SilverBullet's api.

They are just throwaway examples, but I'm keeping them around in case I need to experiment more.


To run them use:

```
$ deno run --allow-run=git some-file.ts
```
